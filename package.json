{
  "name": "gatsby-starter-digital-garden",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public -b master",
    "live": "git add . && git commit -m 'Gab' && git push -u origin dev"
  },
  "dependencies": {
    "gatsby": "^2.21.37",
    "gatsby-theme-garden": "^0.1.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "gh-pages": "^3.1.0"
  }
}
